// TODO: 后续接入真实 API 后实现
// 暂时使用硬编码值，避免 API 依赖

/**
 * 获取分类总数（硬编码版本）
 * TODO: 接入真实 API 后替换
 */
export const useCategoriesCount = () => {
  return { data: 0 }; // 硬编码
};

/**
 * 获取子分类总数（硬编码版本）
 * TODO: 接入真实 API 后替换
 */
export const useSubcategoriesCount = () => {
  return { data: 0 }; // 硬编码
};
